{
    "name": "C++ Development Environment",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-vscode.cpptools"
    ],
    "forwardPorts": [8080],
    "postCreateCommand": "cmake --build . --config Release"
}
